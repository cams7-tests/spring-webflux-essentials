name: CI/CD - Development

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build
    runs-on: self-hosted
    env:
      NEXUS3_USERNAME: ${{ secrets.NEXUS3_USERNAME }}
      NEXUS3_PASSWORD: ${{ secrets.NEXUS3_PASSWORD }}
      R2DBC_DATABASE_URL: postgres-svc
      R2DBC_DATABASE_USERNAME: spring_webflux_essentials
      R2DBC_DATABASE_PASSWORD: abc12345
      LOGGING: DEBUG

    steps:
    - id: checkout
      name: Checkout
      uses: actions/checkout@v2
      with:
        fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

    - id: build-maven-app
      name: Buid Maven application
      uses: cams7-devops/cicd/build-maven@99c79f42a1d274c523cd3606b88bc704de915423      
#      with:
#        logging: DEBUG
#        github-token: ${{ secrets.GITHUB_TOKEN }}
#        sonar-token: ${{ secrets.SONARQUBE_TOKEN }}
#        sonar-host-url: ${{ secrets.SONARQUBE_HOST_URL }}
#        nexus3-username: ${{ secrets.NEXUS3_USERNAME }}
#        nexus3-password: ${{ secrets.NEXUS3_PASSWORD }}
#        r2dbc-database-url: ${{ secrets.R2DBC_DATABASE_URL }}
#        r2dbc-database-username: ${{ secrets.R2DBC_DATABASE_USERNAME }}
#        r2dbc-database-password: ${{ secrets.R2DBC_DATABASE_PASSWORD }}
    
    
